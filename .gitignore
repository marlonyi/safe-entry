# ── Dependencias ──────────────────────────────────────────────
node_modules/

# ── Variables de entorno (NUNCA subir secretos) ───────────────
.env
.env.docker
.env.local
.env.production
.env.staging
.env.test
# Permitir archivos de ejemplo (sin secretos reales)
!.env.example
!.env.local.example
!.env.docker.example

# ── Config MCP local (contiene credenciales — NUNCA subir) ─────
.mcp.json
!.mcp.json.example

# ── Logs ──────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*

# ── Builds y temporales ───────────────────────────────────────
dist/
build/
tmp/
temp/
.cache/
coverage/

# ── Uploads del usuario (datos en producción) ─────────────────
uploads/

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.so
venv/
ENV/
.venv/
env/
*.egg-info/
.pytest_cache/

# ── Modelos de ML (archivos grandes, usar Git LFS si se necesitan) ──
*.pt
*.onnx
*.arff
*.h5
*.pkl

# ── Sistema Operativo ─────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# ── IDEs ──────────────────────────────────────────────────────
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.swp

# ── Docker ────────────────────────────────────────────────────
# (Los docker-compose sí van al repo, solo ignorar overrides locales)
docker-compose.override.yml

# ── Certificados y claves ─────────────────────────────────────
*.pem
*.key
*.crt
*.p12
*.pfx

# ── Misceláneos ───────────────────────────────────────────────
*.tgz
*.tar.gz
.npmrc
